# Copy to .env on the VPS and fill in. Never commit .env — this repo is public.
#
# Read-only gateway token, scoped to /prices. Same value as
# GATEWAY_READONLY_TOKEN in the trading platform's /opt/fix-gateway/.env.
GATEWAY_READONLY_TOKEN=
